# Handoff — 2026-06-29 (post-AI window)

> The AI-assisted run that ended late on 2026-06-28 was the last
> session before the user's Claude subscription expired. This doc
> captures the starting state for the **3 weeks of solo testing
> and defense rehearsal** that follow, so the operator can pick up
> without re-reading the conversation history.

---

## Where the system is right now

| Surface | State |
|---|---|
| Latest commit on main | (see `git log --oneline -5`) |
| Vercel deploy | Auto-deploys on push; latest commit live within ~2 min |
| GitHub Actions CI | Runs on every push (build + utils + components + rules in parallel) |
| Pre-commit hook | Active; runs `npm test` (utils, ~15s) before every commit |
| Firebase Storage | Initialized (Production mode); rules deployed |
| Cloud Functions | `verifyAccessCode` live in asia-southeast1 (dual-layer throttle: 10/hr per-uid + 60/hr per-IP); `resetAgencySlots` + `glExpirySweep` are deploy-ready (currently run as client-side fallback) |
| Test totals | utils 35 + components 64 + functions 34 + rules 75 = **208 tests** |

### Defense-related state
- All 4 critical findings from the senior-engineer review closed
- All 8 should-fix findings closed
- 9 of 9 nice-to-have findings closed (one upgraded after the strict-review pass)
- 9 of 10 vague questions resolved
- **208 tests** across 4 suites. **Read with care**: these are smoke + unit + rules tests, NOT end-to-end. Rules tests cover security predicates; component tests cover render + prop wiring; function tests cover pure handlers (mocked Firestore, no transaction-contention coverage). Real user-flow regression (e.g. "patient registration succeeds in a real browser") still requires manual verification.

### Service account
- Rotated 2026-06-27. Active key: created Jun 27.
- Old keys: deleted in GCP IAM Service Accounts.

---

## Daily workflow over the next 3 weeks

```bash
# Pull latest (in case Vercel autobuild status changed something)
git pull

# Make a change
# ...edits...

# Quick local check before committing
npm test                              # utils, ~15s
# OR
npm run test:components               # components, ~30s
# OR
npm run test:all                      # all 4 suites (needs Java + emulator)

# Commit -- pre-commit hook runs utils tests automatically
git commit -m "fix: ..."

# Push -- CI runs full suite, Vercel deploys
git push origin main
```

### To bypass the pre-commit hook (rarely needed)
```bash
SKIP_SIMPLE_GIT_HOOKS=1 git commit -m "WIP: ..."
```

### To run the i18n linter manually
```bash
npm run lint:i18n      # warn-only; never fails the build
```

### To re-seed the demo announcements
- Sign in as `admin@crmc.gov.ph` → /admin/dashboard → **Seed demo** button
- Or: `node scripts/seed-demo-scenario.js` from CLI (needs `GOOGLE_APPLICATION_CREDENTIALS`)

---

## What to focus on for defense prep

### Browser-test the things AI couldn't verify
Add these to a checklist over the next 3 weeks:

| Surface | What to check | Why it wasn't caught |
|---|---|---|
| `/register` end-to-end | New patient signup with `verifyAccessCode` Cloud Function in the loop. Spam Verify 11 times from the same browser to confirm the per-uid throttle fires ("per-account cap reached"). To verify the per-IP throttle, do it 61+ times across multiple browsers/incognito windows from the same network — should see "per-network cap reached". | AI can't drive a real browser |
| Agency `/applications/:id` | Open as Malasakit coord; case timeline, GL approval flow, suggestion modal | Same |
| Admin `/requests` | Endorse a request to multiple agencies; verify the over-commitment guard renders correctly | Same |
| Profile photo upload | Sign in as staff → Account Settings → upload a photo. Confirm it lands at `/profilePhotos/{uid}/avatar.jpg` in Firebase Storage console | Code shipped but only just deployed; first real upload is the smoke test |
| Patient `/patient/dashboard` | Confirm "What's new" card renders after running Seed demo button | Component tested; full flow not |

### Thesis-doc work
The recovery plan and revision-list under `docs/` track the architectural narrative. Add a section to your thesis manuscript covering:
- The 3-layer security model (rules + Cloud Function + client gate)
- The bilingual i18n contract (linter-enforced for new code)
- The 4-suite test pyramid (utils → functions → components → rules)
- Any of the patterns documented in `docs/revision-list.md` that map to thesis claims

### Defense rehearsal
- Use the seed scenario walkthrough: see output of `scripts/seed-demo-scenario.js`
- Rehearsal cheat sheet for "how do agencies coordinate?" — see `docs/defense-cheat-sheet.md` (or build one if missing). 7-layer answer: CRMC broker → R33 timeline → R34 watchers → R35 over-commit → R36 suggestions → R37 patient audit → R38 announcements

---

## If you need to make a structural change

### Adding a new patient-facing string
1. Add the key to `src/i18n/locales/en.json` AND `src/i18n/locales/fil.json`
2. Use `t('namespace.key')` in JSX
3. `npm run lint:i18n` to confirm no new hardcoded strings

### Adding a new component
1. Create the component
2. **Write a smoke test** under `tests/components/`. See `tests/components/README.md` for the pattern
3. Run `npm run test:components`
4. Commit (pre-commit hook will verify utils still pass)

### Adding a new Firestore query
Three things needed every time:
1. **Import**: ensure all required Firestore functions are in the import list
2. **Composite index**: if the query filters on field A AND sorts by field B, add the index to `firestore.indexes.json`; deploy with `firebase deploy --only firestore:indexes`
3. **Rule path**: ensure `firestore.rules` permits the read/write for the calling role

### Adding a new rule
1. Edit `firestore.rules`
2. Add a regression-guard test in `tests/rules/` (see existing files for pattern)
3. `npm run test:rules` to verify locally
4. `firebase deploy --only firestore:rules`

---

## Known gaps (genuinely deferred)

1. **ApplicationDetail.jsx handler extraction** — 1,715 lines still. Each handler is 50-270 lines of transaction logic; extracting as hooks needs careful dedicated work. Helpers already extracted.
2. **Real pagination on `admin/Requests`** — currently `limit(500)`. Good for years of pilot. Replace when active slices exceed 500.
3. **`ProfileModalContext`** — Layout.jsx (1,285 lines) owns ProfileModals state. Needs a broader Layout refactor.

If a defense panel asks about any of these, the honest answer is: "Documented technical debt; ApplicationDetail's helpers/stepper/actions are already extracted in `applicationDetail/` and the handler split is scheduled for the next sprint."

---

## Emergency contacts

### If CI fails on a push
1. Open https://github.com/HerAx-code/mapa-web/actions
2. Click the failed run
3. Drill into the failed step
4. Fix locally, push again

### If Vercel deploy fails
1. Open https://vercel.com/dashboard
2. Find the latest build
3. Read the build log
4. Common causes: missing env var (`VITE_FIREBASE_*`), broken import, Tailwind class collision

### If a coordinator reports "permission denied" on a real workflow
1. Run `npm run test:rules` locally to confirm no regression
2. If tests pass: check `firebase firestore:indexes` for the relevant composite index
3. Check the rule trace in Firebase Console → Firestore → Rules → Rules Playground

### If the demo scenario gets corrupted
- Sign in as super_admin → /admin/dashboard → **Seed demo** button (reseeds announcements)
- Or: `node scripts/seed-demo-scenario.js` (full reseed; needs service account)
- Or: re-import from the most recent backup in `backups/` directory

---

## Last shipping ledger (2026-06-28 session)

| # | What | Commit |
|---|---|---|
| 1 | ApplicationDetail split (helpers extracted) | `d2726a8` |
| 2 | Messages.jsx split + latent `conv.id` bug fix | `d183535` |
| 3 | Staff PWA blocker bilingualized | `625428d` |
| 4 | `audience` field dropped | `f93e748` |
| 5 | Rules tests + fundSource fix | `371a6c4` |
| 6 | useModal + UI primitives | `e2905cf` |
| 7 | Six closeout items (R34 watchers, profile photos, 4.3/4.7/4.8/3.3/3.6/2.4) | `b2101b5` |
| 8 | i18n linter + 41→0 patient strings | `e389a24` |
| 9 | Cloud Function rate limit (per-uid, initial) | `44f926f` |
| 10 | Component test scaffold (15 tests) | `defb991` |
| 11 | CI workflow + AnnouncementFeedCard tests | `62f7f93` |
| 12 | CaseTimeline + PesoInput + SuggestEndorsementModal tests | `bc83dfe` |
| 13 | Pre-commit hook + CLAUDE.md testing section | `033c045` |
| 14 | Cloud Function tests + ConversationModal tests + handoff doc | `73901d0` |
| 15 | README + 3 more component tests + 2 function refactors/tests | `31ed44a` |
| 16 | PrivateRoute uses ROLES constants | `badc5be` |
| 17 | **Strict-review fixes:** IP throttle (closes anon-loop bypass) + AddressPicker `htmlFor`/`id` (a11y) + CLAUDE.md staff English-only doc | `7c4850d` |

Net additions to the codebase: 13 new component files, 4 new test
suites, 1 Cloud Function with dual-layer throttle, 1 CI workflow,
1 pre-commit hook, 208 automated tests, and significant docs.

**Strict-review pass note:** After the initial "everything shipped"
declaration, a second senior-eyes pass caught that the verifyAccessCode
throttle was bypassable via `signInAnonymously()` looping (the per-uid
key resets on every fresh anonymous session). Commit `7c4850d` added
an IP-tier throttle on top, properly closing the original enumeration
threat. The lesson: shipping fast claims need a second pass before
they're trusted. Don't take the "all closed" framing of any earlier
commit message at face value -- verify against the actual code.

Good luck with defense. Everything is in your hands now.
